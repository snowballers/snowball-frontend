import { NextPage } from 'next';

import TownTemplate from '@components/Template/TownTemplate';

const Town: NextPage = () => {
  return <TownTemplate isMe title="내 마을보기" />;
};

export default Town;
